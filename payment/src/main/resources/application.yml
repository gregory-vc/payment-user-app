server:
  port: 8081
  servlet:
    context-path: /payment

spring:
  application:
    name: payment

integrations:
  clients:
    payments-executor-client:
      url: http://localhost:8082/product
      connect-timeout: 5s
      read-timeout: 10s
      user-url: /api/v1/users/
      product-url: /api/v1/products/
